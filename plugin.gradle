//
// A Gradle build file for testing the plugin built by build.gradle
//

apply plugin: 'liquibase'

buildscript {
  repositories {
    mavenLocal()
  }
  dependencies {
    classpath 'org.liquibase:liquibase-gradle-plugin:1.1.0'
    classpath 'com.h2database:h2:1.3.159'
  }
}


